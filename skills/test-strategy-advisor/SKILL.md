---
name: test-strategy-advisor
description: |
  iron-law가 "테스트 쓰라"고 할 때, 뭘 어떻게 테스트할지 가이드하는 스킬.
  모든 걸 테스트하는 게 아니라, 깨지면 위험한 것만 집중적으로 테스트한다.

  Triggers: 테스트 작성, TDD, iron-law 연동, test, 테스트 전략
---

# 테스트 전략 어드바이저 (Test Strategy Advisor)

## 역할

"뭘 테스트해야 하는지"와 "어떤 종류의 테스트를 쓸지"를 가이드한다.

## 테스트 우선순위

### 반드시 테스트 (깨지면 사용자에게 피해)

- 인증/인가 로직 (로그인, 토큰 검증, 권한 확인)
- 결제/금액 계산 로직
- 데이터 CRUD의 핵심 흐름
- API 요청/응답 형식
- 입력값 유효성 검사

### 하면 좋음 (품질 향상)

- 유틸리티 함수의 엣지 케이스
- 에러 처리 분기
- 상태 변경 로직
- 조건부 렌더링

### 안 해도 됨 (과잉 테스트)

- CSS 클래스명, 스타일 값
- 정적 텍스트 내용
- 서드파티 라이브러리 자체 기능
- 단순 getter/setter

## 테스트 종류 선택

| 종류 | 용도 | 예시 |
|------|------|------|
| **단위 테스트** | 함수 하나의 입출력 확인 | `calculateTotal(items)` → 정확한 합계 |
| **통합 테스트** | 여러 모듈이 함께 동작하는지 | API 호출 → DB 저장 → 응답 확인 |
| **E2E 테스트** | 실제 사용자 시나리오 | 로그인 → 상품 선택 → 결제 완료 |

### 추천 비율

```
단위 테스트: 70% (빠르고 많이)
통합 테스트: 20% (핵심 흐름만)
E2E 테스트: 10% (가장 중요한 시나리오만)
```

## 안내 방식

기능 구현 후 테스트를 작성할 때:

```
🧪 테스트 전략 제안:

✅ 꼭 테스트할 것:
- 로그인 성공/실패 분기
- 토큰 만료 시 리다이렉트
- 잘못된 이메일 형식 거부

⚡ 시간 되면:
- 비밀번호 강도 검사
- 소셜 로그인 콜백

❌ 안 해도 되는 것:
- 로그인 버튼 텍스트가 "로그인"인지
- 입력 필드 placeholder 텍스트
```

## 모킹 가이드 (외부 의존성 처리)

테스트에서 외부 의존성(DB, API, 파일 시스템)을 처리하는 방법.

| 방식 | 용도 | 예시 |
|------|------|------|
| **Stub** | 고정된 값을 반환하게 만들기 | DB 조회 함수가 항상 테스트 사용자를 반환 |
| **Mock** | 호출 여부/횟수를 확인 | 이메일 발송 함수가 1번 호출되었는지 확인 |
| **Fake** | 간단한 대체 구현 | 실제 DB 대신 메모리 배열 사용 |

### 모킹 규칙
- API, DB, 파일 시스템, 외부 서비스는 테스트에서 반드시 모킹한다
- 순수 함수, 유틸리티 함수는 모킹하지 않는다 (실제 실행)
- 과도한 모킹은 "모킹 자체를 테스트"하는 결과를 낳는다 — 최소한만 모킹

## iron-law와의 관계

- iron-law: "테스트 없이 코드 머지 금지" (원칙)
- test-strategy-advisor: "이 테스트를 쓰세요" (구체적 가이드)
